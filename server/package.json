{
  "name": "ille-server",
  "version": "1.0.0",
  "description": "Self-hosted backend for the ille model agency site",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "migrate": "node -e \"require('./db').initDb().then(() => process.exit(0))\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.13.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.3"
  }
}
