// GitHub Pages build — frontend at https://jeanmicheltawk.github.io/ille/
// API must run elsewhere (e.g. Render). Update this URL after you deploy the server.
export const environment = {
  production: true,
  apiUrl: 'https://ille-api.onrender.com/api',
};
